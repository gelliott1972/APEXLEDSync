# API Configuration
VITE_API_URL=http://localhost:4000

# App URL (for invite messages - defaults to window.location.origin if not set)
VITE_APP_URL=

# AWS Cognito (for local development)
VITE_USER_POOL_ID=local-user-pool
VITE_USER_POOL_CLIENT_ID=local-client-id
